<Page x:Class="ExpertService.PagesFolder.Dashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ExpertService.PagesFolder"
      d:DesignHeight="450" d:DesignWidth="800"
      mc:Ignorable="d" 
      Title="Dashboard">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#F5F5F5">
        <StackPanel Margin="20">

            <!-- Заголовок -->
            <TextBlock Text="Главный экран (Индикаторы)" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333"/>

            <!-- Блок с KPI-карточками (Большие цифры) -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Карточка 1: Всего заказов -->
                <Border Grid.Column="0" Background="#3498db" CornerRadius="10" Margin="0,0,20,0" Padding="15">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" BlurRadius="5" Opacity="0.4"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Всего заказов" FontSize="14" Foreground="White"/>
                        <TextBlock Name="TxtTotalOrders" Text="0" FontSize="40" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>

                <!-- Карточка 2: Новых за сегодня -->
                <Border Grid.Column="1" Background="#e74c3c" CornerRadius="10" Margin="0,0,20,0" Padding="15">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" BlurRadius="5" Opacity="0.4"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Новых за сегодня" FontSize="14" Foreground="White"/>
                        <TextBlock Name="TxtOrdersToday" Text="0" FontSize="40" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>

                <!-- Карточка 3: В работе (сейчас) -->
                <Border Grid.Column="2" Background="#f1c40f" CornerRadius="10" Padding="15">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" BlurRadius="5" Opacity="0.4"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Заказов в работе" FontSize="14" Foreground="White"/>
                        <TextBlock Name="TxtOrdersInProgress" Text="0" FontSize="40" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Блок с Прогресс-барами (Визуализация статусов) -->
            <TextBlock Text="Процентное соотношение статусов" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

            <StackPanel Name="StatusStackPanel">
                <!-- Сюда будут динамически добавляться прогресс-бары из кода C# -->
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
